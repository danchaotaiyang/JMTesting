import {postMode} from '@/assets/js/config';

const state = {
    code: null,
    token: null,
    userInfo: null,
    params: postMode.params,
    paperType: 0,
    hasAnswer: 0,
    isAssignment: 0,
    curIndex: 1,
    viewCard: false,
};

export default state;
